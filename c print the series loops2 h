#include<stdio.h>
#include<math.h>
int main()
{
    int n;
    scanf("%d",&n);
    int term;
    if(n%2==0)
    {
          term=(n-1)/2;
        printf("%d",term);
    }
    else
    {
       term=n-1;
        printf("%d",term);
    }
    return 0;
}
