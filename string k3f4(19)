#include<stdio.h>
#include<string.h>
int main()
{
	char str[50];
	int i,n,j;
	scanf("%[^\n]s",str);
	n=strlen(str);
	for(i=1;i<n;i=i+2)
	{
	   int r=(int)str[i]-48;
	for(j=0;j<r;j++)
		{
		printf("%c",str[i-1]);
		}

	}

}
