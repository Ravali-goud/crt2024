#include <stdio.h>
int reverse(int n)
{
    int d,r=0;
    
    while(n>0)
    {
        d=n%10;
        n=n/10;
        r=r*10+d;
    }
  printf("%d",r);
}

int main() {
    // Write C code here
    int n;
    scanf("%d",&n);
    reverse(n);
 
    return 0;
}
