//sum of digits in a given numbers
#include <stdio.h>
int sumofdigit(int n)
{
    if(n==0)
    return 0;
    else
    return n%10+sumofdigit(n/10);
}

int main()
{
    int n;
    scanf("%d",&n);
    int r=sumofdigit(n);
    printf("%d",r);
}
