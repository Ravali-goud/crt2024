#include <stdio.h>
int reverse()
{
    int n,d,r=0;
    scanf("%d",&n);
    while(n>0)
    {
        d=n%10;
        n=n/10;
        r=r*10+d;
    }
   return r;
}

int main() {
    // Write C code here
    int k=reverse();
 printf("%d",k);
    return 0;
}
