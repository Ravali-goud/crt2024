#include <stdio.h>

int main() {
    // Write C code here
   int a;
   scanf("%d",&a);
   int r=a%4;
   int x=a%100;
   if(r==0&&(x!=0||a%400==0))
   {
       printf("leap year");
   }
   else
   {
       printf("not a leap year");
   }
    return 0;
}
