#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */ 
   int a,b,count=0,i,j;
    scanf("%d%d",&a,&b);
  for(j=a;j<=b;j++)
  {
      count=0;
      for(i=1;i<=j;i++)
   {
          
       if(j%i==0)
       
        count++;
          
    
 }  
    if(count==2)
    {
        printf("%d ",j);
    }
    
   } 
    return 0;
}
